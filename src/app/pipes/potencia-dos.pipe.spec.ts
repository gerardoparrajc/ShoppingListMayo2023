import { PotenciaDosPipe } from './potencia-dos.pipe';

describe('PotenciaDosPipe', () => {
  it('create an instance', () => {
    const pipe = new PotenciaDosPipe();
    expect(pipe).toBeTruthy();
  });
});
